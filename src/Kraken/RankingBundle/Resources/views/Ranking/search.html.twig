{% extends '::base_ranking.html.twig' %}

{% form_theme form 'KrakenRankingBundle:Form:form_div_layout.html.twig' %}

{% block page_title %}Wyszukiwanie{% endblock page_title %}

{% block body %}
<div class="row">
    <div class="col-sm-4">
        <form method="POST" action="{{ url('ranking_search')}}">
            {{ form_rest(form) }}
            <button type="submit" class="btn btn-raised btn-primary">Szukaj</button>
        </form>
    </div>
    <div class="col-sm-8">
        {% include('KrakenRankingBundle:Ranking:boilerList.html.twig') with {boilers: boilers, search: search} %}
    </div>
</div>
{% endblock body %}
